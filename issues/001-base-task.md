# Issue 001: Complete the base task

This repository is a clean base template. The task for an agent is described in `benchmark/task.md`.

Expected outcome:

- Read the repository instructions.
- Run the documented validation command.
- Make only task-relevant changes.
- Save generated reports under `reports/` when applicable.
